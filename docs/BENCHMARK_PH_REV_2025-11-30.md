# pH Automation Benchmark – 2025-11-30

Purpose: Freeze a high-confidence snapshot of the working pH auto‑dosing revision (PR #88) before beginning EC automation refactors. This document captures configuration, controller status, recent dose behavior, and retrieval commands so the state can be reconstituted or compared later.

## Revision
- Branch: `copilot/sweet-cat`
- Active PR: #88 (pH chart + auto dosing + safety + chiller protection)
- Git commit (Pi): (run command below) – expected short SHA near `4a08c4e` or later
- Asset version: via `GET /api/version`

## Canonical Settings Keys (pH)
| Key | Expected Value (today) | Notes |
| --- | --- | --- |
| `targets.ph_low` | 5.2 (example) | Lower bound – adjust per crop |
| `targets.ph_high` | 5.8 (example) | Upper bound – dosing holds when within window |
| `dosing.ph_up_initial_ml` | 0.01 | Conservative first auto dose |
| `dosing.ph_min_interval_s` | 900 | Global cooldown between auto doses |
| `dosing.ph_max_predicted_delta_ph` | 0.3 | Safety cap for model prediction |
| `dosing.ph_stabilization_window_s` | 300 | Wait window for stabilization sampling |
| `dosing.ph_stabilization_delta` | 0.02 | Max delta between samples to declare stable |
| `dosing.ph_stabilization_samples` | 3 | Number of samples inside window |

Record actual exported values (see Commands) into an artifact (JSON) committed or archived.

## Controller Runtime Status (Target Snapshot)
From `GET /api/controllers/status`:
```json
{
  "controllers": {
    "ph": {
      "auto_enabled": true,
      "will_automate": true,
      "holding_reason": null,
      "learned_ml_per_pH": <~dynamic small number>,
      "remaining_cooldown_s": <~0..900>,
      "guards": {"estop": false, "sensor_stale": false, "reservoir": false},
      "targets": {"low": 5.2, "high": 5.8},
      "ph": 5.22
    }
  }
}
```

Acceptance criteria captured here: single Parameters panel in UI, no duplicate JS safety panel, dose events show consistent 0.01 ml entries spaced ≥900s apart unless manual.

## Dose Event Behavior
- Table: `dose_events` is source of truth (unified logging).
- Auto entries example: reason `auto_ph`, actor `controller`, ml ~0.01, blocked_by null.
- Cooldown respected: next auto dose only after `dosing.ph_min_interval_s`.

## Benchmark Data Collection Procedure
1. Confirm commit & version:
   ```powershell
   ssh pi@<PI_HOST> "cd ~/RDWC-v4; git rev-parse --short HEAD"
   Invoke-RestMethod http://<PI_HOST>:8080/api/version
   ```
2. Capture controllers + health:
   ```powershell
   Invoke-RestMethod http://<PI_HOST>:8080/api/controllers/status | ConvertTo-Json -Depth 6 > controllers_status.json
   Invoke-RestMethod http://<PI_HOST>:8080/api/health | ConvertTo-Json -Depth 4 > health.json
   ```
3. Export settings and select pH subset:
   ```powershell
   $settings = Invoke-RestMethod http://<PI_HOST>:8080/api/settings/export
   $settings | ConvertTo-Json -Depth 4 > settings_full.json
   $settings | Select-Object targets.ph_low, targets.ph_high, dosing.ph_up_initial_ml, dosing.ph_min_interval_s, dosing.ph_max_predicted_delta_ph, dosing.ph_stabilization_window_s, dosing.ph_stabilization_delta, dosing.ph_stabilization_samples | ConvertTo-Json > settings_ph.json
   ```
4. Recent dose events (last 6h) + raw recent unified trend:
   ```powershell
   Invoke-RestMethod "http://<PI_HOST>:8080/api/dose/recent?hours=6" | ConvertTo-Json -Depth 6 > doses_6h.json
   Invoke-RestMethod "http://<PI_HOST>:8080/api/trends?gran=300&max=500" | ConvertTo-Json -Depth 6 > trends_ph_ec_temp.json
   ```
5. Consolidate into archive folder (optional) and commit.

## Optional Script
See `tools/benchmark_ph.ps1` (added in this revision) for automated capture.

## Restoration Notes
- If moving to EC development, keep these pH keys untouched unless an EC change requires cross‑controller guard interaction.
- Avoid reintroducing legacy fallback defaults (e.g., learned pH ml/pH resetting to 5.0 baseline).
- Re-run benchmark after EC automation merges to ensure pH remains stable (compare trends + dose cadence).

## Risk Register (pH)
- Learned ml/pH drifting upward due to sensor noise – mitigated by stabilization window & small initial dose.
- Duplicate UI panels causing conflicting writes – resolved (verify absence of JS injected panel).
- Cooldown bypass via manual dosing – acceptable; manual events logged distinctly.
- Sensor stale guard false negative – monitor `guards.sensor_stale` in controllers status.

## Next Steps
1. Merge PR #88 after review passes.
2. Tag or note commit SHA in `VERSION` if semantic release desired.
3. Begin EC automation refactor with reference to this baseline.

---
Document generated by housekeeping agent on 2025-11-30.
