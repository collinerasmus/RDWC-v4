═══════════════════════════════════════════════════════════
  RDWC-v4 MODE SYSTEM FIX - DEPLOYMENT INSTRUCTIONS
═══════════════════════════════════════════════════════════

STEP 1: ON YOUR WINDOWS PC (Right now)
───────────────────────────────────────────────────────────
Open PowerShell in: c:\Users\USER-PC\OneDrive\Documents\GitHub\RDWC-v4

Run these commands:

    git add -A
    git commit -m "CRITICAL FIX: Unify mode system - single source of truth"
    git push


STEP 2: ON YOUR RASPBERRY PI (SSH from Windows)
───────────────────────────────────────────────────────────
Open a new PowerShell and run:

    ssh pi@192.168.88.49

Then on the Pi, run:

    cd /home/pi/rdwc-v4
    git pull
    sudo systemctl restart rdwc-api
    sudo systemctl restart rdwc-sensors
    sudo systemctl status rdwc-api


STEP 3: ON YOUR HMI LAPTOP (192.168.88.33)
───────────────────────────────────────────────────────────
1. Open browser to: http://192.168.88.49:8080
2. Press: Ctrl + Shift + F5 (hard refresh)
3. Click the "MANUAL" button in the header
4. CHECK: Do ALL tabs now show "Resume" buttons?
   - pH tab
   - EC tab  
   - Circulation tab
   - Lights tab
   - Chiller tab
5. Click the "AUTO" button in the header
6. CHECK: Do ALL tabs now show "Hold" buttons?


VERIFICATION TESTS:
───────────────────────────────────────────────────────────
✓ PASS: All tabs change when header mode changes
✓ PASS: Individual "Hold" button pauses only that controller
✓ PASS: "Resume" button restores that controller

✗ FAIL: If mode changes don't propagate, tell me what you see


WHAT THIS FIXED:
───────────────────────────────────────────────────────────
BEFORE: 4 duplicate mode systems → mode changes lost
AFTER: 1 unified mode system → mode changes work instantly


IF IT WORKS:
───────────────────────────────────────────────────────────
Tell me "IT WORKS!" and I'll help you with the next fixes:
- Browser connection stability
- Relay buttons in System tab
- Cache busting for JS files


IF IT DOESN'T WORK:
───────────────────────────────────────────────────────────
Tell me exactly what happened:
- Which button did you click?
- What did you expect?
- What actually happened?
- Any error messages in browser console? (F12)


═══════════════════════════════════════════════════════════
