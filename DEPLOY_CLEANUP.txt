═══════════════════════════════════════════════════════════
  RDWC-v4 CLEANUP DEPLOYMENT - Run These Commands
═══════════════════════════════════════════════════════════

SITUATION:
- System is working (sensors online, API responding)
- Browser shows "offline" because page stayed open overnight
- System is in MANUAL mode (that's why main pump is off)
- Code cleanup completed (3 duplicate mode systems removed)

═══════════════════════════════════════════════════════════
STEP 1: FIX THE PI (In PuTTY)
═══════════════════════════════════════════════════════════

Open PuTTY, connect to: 192.168.88.49
Login as: pi

Then run these commands ONE BY ONE:

cd /home/pi/rdwc-v4

git status

git pull

sudo systemctl restart rdwc-api

sudo systemctl restart rdwc-sensors

sudo systemctl status rdwc-api

# Press 'q' to exit status view

# Check the logs for any errors:
sudo journalctl -u rdwc.service -n 50 --no-pager

═══════════════════════════════════════════════════════════
STEP 2: FIX THE HMI BROWSER
═══════════════════════════════════════════════════════════

On your HMI laptop (192.168.88.33):

1. Close the browser completely (all tabs)
2. Reopen browser
3. Go to: http://192.168.88.49:8080
4. Press Ctrl+Shift+F5 (hard refresh)

Everything should now show as online with live readings.

═══════════════════════════════════════════════════════════
STEP 3: FIX THE MAIN PUMP (Switch to AUTO)
═══════════════════════════════════════════════════════════

The system is in MANUAL mode. To enable automation:

In the web UI:
1. Click the "AUTO" button in the header
2. Verify all controllers show "Hold" buttons
3. Go to System tab
4. The main pump should now respond to scheduler/automation

OR via PuTTY (if you prefer command line):

curl -X POST http://localhost:8080/api/system_mode \
  -H "Content-Type: application/json" \
  -d '{"mode":"auto"}'

Then check:
curl http://localhost:8080/api/controller/modes | python3 -m json.tool

═══════════════════════════════════════════════════════════
STEP 4: VERIFY EVERYTHING WORKS
═══════════════════════════════════════════════════════════

On HMI web UI, check:

✓ Top right shows "Online" (not "Offline")
✓ Sensors tab shows live temperature/pH/EC updating
✓ System tab shows relay controls
✓ Camera feed loads (if camera enabled)
✓ Mode buttons (AUTO/MANUAL) work
✓ Individual Hold/Resume buttons work on each tab

═══════════════════════════════════════════════════════════
WHAT WAS FIXED
═══════════════════════════════════════════════════════════

✓ Removed 3 duplicate mode systems (unified_mode is now sole source)
✓ Fixed chiller_control.py bug (undefined 'state' variable)
✓ Updated all tests to use unified mode API
✓ 143 tests passing
✓ All backend endpoints verified working

═══════════════════════════════════════════════════════════
IF PROBLEMS PERSIST
═══════════════════════════════════════════════════════════

1. Check API is running:
   curl http://192.168.88.49:8080/api/relays/status

2. Check sensor poller:
   sudo systemctl status rdwc-sensors

3. View recent errors:
   sudo journalctl -u rdwc.service -n 100 --no-pager | grep -i error

4. Restart everything:
   sudo systemctl restart rdwc.service rdwc-sensors
   
Tell me the error messages and I'll fix them.

═══════════════════════════════════════════════════════════
