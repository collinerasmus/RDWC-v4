[pytest]
# Only collect tests from the dedicated tests/ directory to avoid hardware deep diagnostics and duplicates
testpaths = tests
addopts = -q
filterwarnings = ignore::DeprecationWarning
markers =
	smoke: quick functional smoke test subset
