I need you to implement a simplified control model and polished HMI in the RDWC-v4 repository on branch `copilot/review-auto-enable-system`, now that POST response issues are fixed (commit 92b1cb4). This work replaces per-controller auto/hold with a single System Auto toggle, gates safeties to Auto mode, and applies a dark hydroponic/tech theme. Staged rollout with clear acceptance criteria.

## Objectives

1. **Single System Auto** – remove per-controller auto/hold complexity
2. **Auto-only safeties** – manual mode is unrestricted (except hard protections)
3. **Polished HMI** – dark hydroponic/tech theme, unified controls
4. **Clean database & code** – remove legacy mode/hold settings and UI
5. **Clean tests & docs** – update tests, remove obsolete ones, refresh docs

## Context: POST Response Transmission (RESOLVED)

**Symptom**: POST requests timeout after 2-3 seconds, but database changes ARE applied successfully. HTTP response never sent.

**CRITICAL DISCOVERY**:
- POST endpoints DO execute (proven by database state changes after timeout)
- Database writes complete successfully
- HTTP response is NEVER sent back to client
- POST requests do NOT appear in uvicorn logs (GET requests do)

**Evidence**:
```bash
# POST times out
curl -X POST http://localhost:8080/api/auto/global -d '{"enabled": false}' --max-time 2
# Result: timeout after 2s, NO response

# BUT database WAS changed!
sqlite3 data/rdwc.db "SELECT value FROM settings WHERE key='controls.global_auto';"
# Result: false  ← CHANGE WAS APPLIED!

# POST requests not logged
journalctl -u rdwc.service --since '2 minutes ago' | grep POST
# Result: NOTHING (but GET requests are logged)
```

**Fix**: Commit 92b1cb4 removed body reading in RequestAuditMiddleware; POST now returns in ~84ms.

**Your tasks**:
1. Add comprehensive logging to POST endpoints (start/middle/end) to prove execution
2. Investigate why responses aren't transmitted despite successful execution
3. Remove `get_auto_status()` from return statement temporarily and test
4. Convert endpoints to async: `async def api_auto_global_set(...)`
5. Check FastAPI middleware stack for response blocking
6. Investigate uvicorn `timeout-keep-alive=5` interaction
7. Test if response JSON serialization fails silently

**Test after fix**:
```bash
curl -X POST http://localhost:8080/api/auto/global \
  -H "Content-Type: application/json" \
  -d '{"enabled": false}'
# Should return {"ok": true, "global_auto": false} in <200ms
```

## Task A: Clean Conflicting Settings (CRITICAL)

**Problem**: Database has BOTH old mode system AND new auto system, causing state confusion.

**Evidence from database**:
```
chiller.auto_enabled|1                    ← OLD scattered setting
controller.chiller.mode|auto              ← OLD relay mode system
controls.chiller_auto|true                ← NEW unified setting
controls.global_auto|false                ← NEW global switch
ec.auto_enabled|true                      ← OLD scattered setting
ph.auto_enabled|true                      ← OLD scattered setting
```

**API shows**: `{"mode": "auto"}` from old relay system AND `{"global_auto": false}` from new system

**User observation**: "Chiller appears in auto on frontend but backend shows conflicting state"

**Your tasks**:
1. Remove old scattered settings from database:
   ```sql
   DELETE FROM settings WHERE key IN ('ph.auto_enabled', 'ec.auto_enabled', 'chiller.auto_enabled');
   DELETE FROM settings WHERE key LIKE 'controller.%.mode';
   DELETE FROM settings WHERE key LIKE 'controller.%.held';
   ```
2. Remove legacy mode/hold helpers and reads; update controllers to use `should_automate(controller)` only
3. Verify `app/chiller_control.py`, `app/ph_control.py`, `app/ec_control.py` use `should_automate()` exclusively
4. Remove `get_system_mode()`, `set_system_mode()`, `is_held()`, `set_hold()` functions

## Task B: Frontend – Replace Legacy UI (HIGH)

**Problem**: Frontend at lines 676-679 still has old Auto/Manual/Maintenance buttons, NOT new auto toggles.

**CONFIRMED in code**:
```html
<!-- Lines 676-679 in app/static/index.html -->
<button id="system-mode-auto" class="btn-chip">Auto</button>
<button id="system-mode-manual" class="btn-chip">Manual</button>
<button id="system-mode-maint" class="btn-chip">Maintenance</button>
<!-- OLD systemSetMode() function still wired to these buttons -->
```

**Your tasks**:
1. DELETE old mode buttons from header (lines 676-679)
2. DELETE `systemSetMode()` function and mode sync logic
3. ADD global auto toggle in header:
   ```html
   <button id="global-auto-btn" class="btn-chip">
     🌐 Auto: <span id="global-auto-state">OFF</span>
   </button>
   ```
4. ADD per-controller auto toggles in pH/EC/chiller tabs
5. ADD JavaScript polling: fetch `/api/auto/status` every 2s and update button states
6. Wire button clicks to POST `/api/auto/global`

## Task C: Gate Safeties to Auto Mode (HIGH)

**Your tasks**:
1. In `app/relays_core.py`: enforce anti-flap and non-chiller MIN_ON/OFF only when System Auto ON; keep chiller compressor cooldown always-on; continue reason whitelist with `override` allowed for manual.
2. In `app/dosing.py`, `app/ph_control.py`, `app/ec_control.py`: enforce caps/intervals/stale guards only when System Auto ON; keep always-on safeties (E‑STOP, mixing lock, reservoir>0, hard pH-high ceiling, catastrophic EC-high).
3. In `app/scheduler.py`: execute edges only when System Auto ON; manual lights ON/OFF still permitted via override reason.
4. Extend `/api/auto/status` with `controllers.{name}.reason` codes.

## Task D: HMI Dark Tech Theme (HIGH)

**Theme**: Dark base (slate/navy), aqua/teal accents, muted greens; Inter/Roboto typography; unified button/chip styles; responsive cards/tabs; helpful tooltips.

**Your tasks**:
1. Create `app/static/css/theme.css` and apply consistent variables (colors, spacing, fonts).
2. Standardize status chips across tabs; unify button styles and iconography.
3. Ensure Control vs Settings subtabs are visually distinct and consistent.
4. Validate mobile/low-res usability; ensure high-contrast accessibility.

## Task E: Tests & Housekeeping (MEDIUM)

**Your tasks**:
1. Inventory `tests/`; remove obsolete/brittle tests tied to legacy mode/hold.
2. Add tests for System Auto semantics: manual bypass vs auto guards, POST middleware behavior, controller endpoints.
3. Run linters/formatters; remove unused assets/scripts; update `README.md`, `docs/HMI_SETUP_GUIDE.md`, `docs/OPERATING_MANUAL.md`.

## Task F: Verify Complete Refactoring (HIGH)

**Your tasks**:
1. Search entire codebase for old mode/hold system references:
   - `MODE_AUTO`, `MODE_MANUAL`, `MODE_MAINTENANCE` constants
   - `get_system_mode()`, `set_system_mode()` function calls
   - `ph.auto_enabled`, `ec.auto_enabled`, `chiller.auto_enabled` reads (scattered settings)
   - `controller.*.held`, `controller.*.mode` reads
   - `is_held()`, `set_hold()` function calls
2. Update ALL controllers to ONLY use `should_automate(controller_name)`
3. Verify `app/settings.py` DEFAULTS has ONLY new keys:
   - Keep: `controls.global_auto`, `controls.ph_auto`, `controls.ec_auto`, `controls.chiller_auto`
   - Remove: `ph.auto_enabled`, `ec.auto_enabled`, `chiller.auto_enabled`
4. Check `app/scheduler.py`, `app/relays_core.py` for old mode checks
5. Verify `app/chiller_control.py` uses `should_automate("chiller")` not old settings

## Deliverables

1. **Clean database** - only `controls.global_auto` remains for automation control
2. **Updated frontend** - single System Auto toggle, polished dark tech theme
3. **Auto-only safeties** - manual actions unrestricted (always-on protections remain)
4. **Complete refactoring** - controllers use `should_automate()`, zero legacy references
5. **Working test cases** - POST endpoints respond in <200ms; manual vs auto behavior validated

## Files to Focus On (Priority Order)

**CRITICAL (do these first)**:
1. `data/rdwc.db` - Clean old settings (SQL DELETE statements)
2. `app/static/index.html` lines 676-679 - Remove old mode buttons, add auto toggle
3. `app/static/index.html` lines 720-760 - Remove systemSetMode() and mode sync
4. `app/relays_core.py`, `app/dosing.py`, `app/ph_control.py`, `app/ec_control.py`, `app/scheduler.py` - Gate safeties to Auto-only as specified

**HIGH (do after critical)**:
5. `app/chiller_control.py` - Verify uses `should_automate("chiller")`
6. `app/ph_control.py` - Remove any `ph.auto_enabled` reads
7. `app/ec_control.py` - Remove any `ec.auto_enabled` reads
8. `app/unified_mode.py` - Remove MODE constants if present
9. `app/settings.py` - Clean DEFAULTS dict

**VERIFY (sweep entire codebase)**:
10. Search for: `get_system_mode`, `set_system_mode`, `is_held`, `set_hold`
11. Search for: `ph.auto_enabled`, `ec.auto_enabled`, `chiller.auto_enabled`
12. Search for: `controller.*.mode`, `controller.*.held`
13. Search for: `MODE_AUTO`, `MODE_MANUAL`, `MODE_MAINTENANCE`

## Success Criteria

- ✅ POST /api/auto/* respond in <200ms; GET status reflects changes promptly
- ✅ Database contains ONLY `controls.global_auto` (legacy keys removed)
- ✅ UI shows single System Auto toggle; polished dark tech theme across HMI
- ✅ Manual actions work immediately; Auto-only safeties enforced only when Auto ON
- ✅ Controllers use ONLY `should_automate()`; zero legacy references remain
- ✅ Tests pass for new semantics; obsolete tests removed

## Work Order

1. **First**: Fix POST response issue (add logging, test async, fix transmission)
2. **Second**: Clean database (remove old settings via SQL)
3. **Third**: Update frontend (remove old UI, add new toggles with polling)
4. **Fourth**: Complete refactoring (update all controller code)
5. **Fifth**: Verification sweep (search and destroy old references)

Start with POST response issue - it blocks all testing. Read COPILOT_URGENT_REVIEW.md for full technical details.
